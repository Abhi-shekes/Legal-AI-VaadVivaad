# Copy to .env.local for standalone (non-Docker) local development.
# Vite bakes these in at build time -- changing them requires a restart of
# `npm run dev` (or a rebuild for production).

VITE_API_URL=http://localhost:8000
VITE_SOCKET_URL=http://localhost:8000
